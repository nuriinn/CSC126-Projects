#include <iostream>
using namespace std;

int main()
{
    cout<<"Student's ID : ";
    cin>>studentId;
    cout<<"Student's name : ";
    cin>>studentName;
    cout<<"Type of study : ";
    cin>>study;
        if(study == 1){
            cout<<"Field of study : ";
            cin>>code;
            if(code == 'A'){
                fees = 60.00;
                discount = 0.12;
            }
            else if (code == 'B'){
                fees = 70.00;
                discount = 0.00;
            }
        else if (study == 2){
            cout<<"Field of study : ";
            cin>>code;
            if(code == 'A'){
                fees = 65.00;
                discount = 0.12;
            }
            else if (code == 'B'){
                fees = 75.00;
                discount = 0.00;
            }
        }
        }
        cout<<"Number of subject : ";
        cin>>subject;

    return 0;
}
double calcCredit ( int double , ){
    for ( int credit = 0 ; credit )
    cout<<"Enter credit hours : " <<
}
